---
description: "Terminal Commands & Testing Workflow với new script architecture"
globs: ["scripts/**/*.sh"]
alwaysApply: true
---
# Terminal Commands & Testing Workflow

## 🔧 Command Execution Rule

Khi cần chạy terminal command, PHẢI sử dụng format này và prioritize script utilities:

### ⚡ Requirements
- LUÔN prioritize existing scripts thay vì raw commands
- LUÔN load environment trước khi chạy scripts
- CHỈ sử dụng direct commands khi không có script utility

### ✅ Examples với New Script Architecture

**Database operations:**
```json
{
"name": "run_terminal_cmd",
"description": "Start database with comprehensive setup and health check",
"parameters": {
"command": "./scripts/start-database.sh --full-default-setup",
"is_background": false,
"require_user_approval": true
}
}
```

**Backend development:**
```json
{
"name": "run_terminal_cmd",
"description": "Start backend with auto-reload and debug logging",
"parameters": {
"command": "./scripts/start-backend.sh",
"is_background": false,
"require_user_approval": true
}
}
```

**Environment validation:**
```json
{
"name": "run_terminal_cmd",
"description": "Validate environment configuration using config loader",
"parameters": {
"command": "source scripts/utils/config-loader.sh && load_env_file && validate_global_config",
"is_background": false,
"require_user_approval": true
}
}
```

**Database health monitoring:**
```json
{
"name": "run_terminal_cmd",
"description": "Run comprehensive database health check with detailed report",
"parameters": {
"command": "./scripts/utils/db/db-health-check.sh",
"is_background": false,
"require_user_approval": true
}
}
```

**Database lifecycle management:**
```json
{
"name": "run_terminal_cmd",
"description": "Reset database with user confirmation prompt",
"parameters": {
"command": "./scripts/utils/db/reset-db.sh",
"is_background": false,
"require_user_approval": true
}
}
```

## 🔄 Testing Workflow: Execute → Analyze → Plan → Auto-fix

Khi testing bất cứ thứ gì, sử dụng loop:

### 1. **Execute** - Chạy terminal command để test
### 2. **Analyze** - Kiểm tra output, logs, errors
### 3. **Plan** - Xác định những gì cần fix
### 4. **Auto-fix** - Implement fixes dựa trên analysis
### 5. **Repeat** - Tiếp tục loop cho đến khi resolve

### 🎯 Testing Examples
- Start server → check logs → fix errors → restart
- Run database script → check output → fix SQL → re-run
- Test API endpoint → analyze response → fix logic → re-test

## 📊 Debugging Priority
1. **Terminal logs** - Primary source of truth
2. **Error messages** - Chi tiết lỗi cụ thể
3. **Status codes** - HTTP/SQL/System response codes
4. **Configuration** - Environment variables, settings
5. **Dependencies** - Missing packages, services

## ❌ Anti-Patterns
- ❌ Wrapper như `"type": "function"`
- ❌ Markdown, text, explanation ngoài JSON
- ❌ Thay đổi `is_background` hoặc `require_user_approval`
